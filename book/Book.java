package book;

public class Book {
	// Comic, Fantasy, Horror 클래스는 Book 클래스에 상속을 받는다.
	
	// Book 클래스는  public 반환타입 없음 read() 메서드를 만든다.
	
	// Comic, Fantasy, Horror 클래스는 read 메서드를 오버라이딩해서 각각 코믹책을 읽는다, 판타지책을 읽는다, 공포책을 읽는다 출력
	
	// Main 클래스에서 배열로 묶어 for 문을 이용해 오버라이딩된 read() 메서드 호출
	
	public void read() {
	}
	
}
